---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: worktrees.
spec:
  group: ""
  names:
    kind: Worktree
    listKind: WorktreeList
    plural: worktrees
    singular: worktree
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: Worktree defines a revision to load and possibly manage in the
          HEGEC
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              ref:
                description: WorktreeReference defines the reference to the revision
                properties:
                  branch:
                    description: Branch explicitly defines the revision as a branch
                    type: string
                  commit:
                    description: Commit explicitly defines the revision as a commit
                      SHA
                    type: string
                  tag:
                    description: Tag explicitly defines the revision as a tag
                    type: string
                type: object
              repositoryRef:
                description: RepositoryRef defines a reference to the repository the
                  worktree belongs to
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - ref
            - repositoryRef
            type: object
        type: object
    served: true
    storage: true
